services:
  - type: web
    name: bda-financial-risk-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt && python train_models.py
    startCommand: uvicorn src.api.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
